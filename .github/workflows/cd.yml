# ==========================================
# CD - Continuous Deployment
# ==========================================
#
# This workflow is intentionally disabled
# for the current assignment.
#
# After CI is working successfully, this
# workflow can be enabled and configured for
# deployment.
#
# Possible future deployment flow:
#
# GitHub
#   ↓
# CI Build
#   ↓
# Self-Hosted Runner
#   ↓
# Build Artifact
#   ↓
# Server
#   ↓
# Deploy
#
# ==========================================

# name: CD - Deploy Frontend

# on:
#   workflow_run:
#     workflows: ["CI - Build and Test"]
#     types:
#       - completed

# jobs:
#   deploy:
#     if: ${{ github.event.workflow_run.conclusion == 'success' }}
#     runs-on: self-hosted

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Install Dependencies
#         run: npm ci

#       - name: Build Frontend
#         run: npm run build

#       - name: Deploy
#         run: |
#           echo "Deployment will be configured later."
#           echo "CD pipeline is currently disabled."